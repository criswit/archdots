#!/bin/bash

# system settings

# disable the annoying bell (which sounds more like a squeak) that sounds when
# an errant movement is made in the shell
gsettings set org.gnome.desktop.wm.preferences audible-bell false
